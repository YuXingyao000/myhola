# @package _global_
defaults:
  - override /model: diffusion_cross_attn
  - override /condition: single_img
  - override /strategy: distillation
  - _self_

trainer:
  batch_size: 16
  learning_rate: 5.0e-5
  gpus: 4
  exp_name: kd_real_photo

dataset:
  name: Diffusion_dataset
  real_photo_ratio: 1.0

strategy:
  teacher:
    checkpoint: ???
